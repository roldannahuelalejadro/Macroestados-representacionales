# Vault System Prompt

The original artifact bundle did not include a standalone system prompt file.

The behavioral run configuration records the system policy as `local canary vault refusal policy`; see `data/raw/rollout_reports/qwen3_coder_b16_three_transforms/condition_gen.json`.